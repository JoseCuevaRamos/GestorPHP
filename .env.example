APP_NAME=conduit
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost
APP_LOG_LEVEL=debug

# Opciones para token de usuario (usar solo en entornos controlados):
# Opción A) Token como JSON crudo (no recomendado si tu .env se comparte):
#GOOGLE_TOKEN_JSON='{"access_token":"ya29...","refresh_token":"1//...","created":...}'
# Opción B) Token codificado en base64 (recomendado si lo pones en .env):
#GOOGLE_TOKEN_JSON_B64=<base64-of-token.json>

# Nota: si usas GOOGLE_TOKEN_JSON/_B64 no habrá persistencia automática en la variable de entorno
# cuando el token se refresque; para persistencia, configure GOOGLE_TOKEN_PATH hacia un archivo
# (por ejemplo un volumen montado) donde la app pueda escribir el token renovado.

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=conduit
DB_USERNAME=conduit_user
DB_PASSWORD=secret

JWT_SECRET=supersecretkeyyoushouldnotcommittogithub
CORS_ALLOWED_ORIGINS="*"

